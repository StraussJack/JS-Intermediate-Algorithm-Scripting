function sumFibs(num) {
    let sum  = 0;          
    let num1 = 0;
    let num2 = 1;
    while(num2 <= num) {
      if(num2 % 2 == 1) {
        sum += num2; 
      }

      num2=num1+num2;
      num1=num2-num1;
      
    }

     return sum;

}


sumFibs(4);
